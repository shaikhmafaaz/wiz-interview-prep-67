
version: '3.8'

services:
  interview-wiz:
    build: .
    ports:
      - "3000:80"
    volumes:
      - ./backend/database:/app/backend/database
    environment:
      - PYTHONPATH=/app/backend
    restart: unless-stopped

volumes:
  database:
    driver: local
